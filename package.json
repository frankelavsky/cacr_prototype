{
  "name": "ccg-dashboard",
  "private": true,
  "type": "module",
  "description": "NLC Child-Centered Governance dashboard. package.json exists only for the dev-time prep script and tests — there is deliberately no build step.",
  "scripts": {
    "data": "node scripts/build-data.mjs",
    "test": "node --test \"scripts/**/*.test.mjs\""
  },
  "devDependencies": {
    "d3-geo": "^3.1.1",
    "topojson-client": "^3.1.0",
    "xlsx": "^0.18.5"
  }
}
